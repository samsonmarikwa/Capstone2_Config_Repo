# this is the port on which our inventory-service will run
server.port=7003

# allow for RefreshScope
management.endpoints.web.exposure.include=*

spring.datasource.url: jdbc:mysql://localhost:3306/inventory_test?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username: root
spring.datasource.password: password
spring.datasource.driver-class-name: com.mysql.jdbc.Driver

# Run in terminal as: curl localhost:7003/actuator/refresh -d {} -H "Content-Type: application/json"
# This will allow refresh
